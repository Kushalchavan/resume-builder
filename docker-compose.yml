services:
  frontend:
    build: ./client
    container_name: resume_frontend
    restart: always
    ports:
      - "5173:5173"

  backend:
    build: ./server
    container_name: resume_backend
    restart: always
    env_file:
      - ./server/.env
    ports:
      - "3000:5000"
